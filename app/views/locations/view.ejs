<%-include('../component/head.ejs')-%>
<%-include('../component/header.ejs')-%> 


<div class="section-entry" style="background-image: url(../img/carousel-2.jpg);">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1>Our Presence</h1>
            </div>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        
        

        <% Locations.forEach(( Location ) => { 
            let parentlocation;
            let childLocation;
            let locationUrl = Location.Url;
            let LocationId = Location._id;

            if( Location.Parent === null ){  
                
                parentlocation = Location.Heading
                 
                %>
                
                <div class="col-lg-12">
                    <div class="btn btn-warning d-block my-2">
                        <a href="<%-locationUrl-%>"><%-parentlocation-%></a>
                    </div>
                </div>
        <% } 
        
        else{
            childLocation = Location.Heading  %> 

            <div class="col-lg-3">
                <div class="btn btn-warning d-block my-2">
                    <a href="<%-locationUrl-%>"><%-childLocation-%></a>
                </div>
            </div>
        
        <% } %>  

        <% }) %>
    </div>
</div>

<%-include('../component/footer.ejs')-%>